import Layout from "../../components/Layout";
import UserForm from "../../components/UserForm";

const UserDetail = () => {
  return <Layout children={<UserForm />} />;
};

export default UserDetail;
